User Authentication ==>
    - register 
    - login 
    - logout (using the  token blacklisting)
    - otp generation for authentication 

Post==>
   - create post 
   - like post 
   - save post 
   - can see feed

User ==>
   - Followers
   - Following
   

schema design 

  user register  
    { 
      username : string ,
      password : string ,
      email : string ,
      profile_img : string
    }

   create post 
    {
      caption : string ,
      post_img : string ,
      post_date : string ,
      user_id : string
       
    } 




     try{
        const {caption , post_url ,user_id} = req.body;

        if(!user_id || !post_url){
            return res.status(400).json({
                message : "required field was missing"
            })
        }
    const postDets = await PostModel.create({
        caption,post_url,user_id
    })

    return res.status(201).json({
        success : true,
        message:"post is created successfully",
        postDets
    })
    }
    catch(error){
        return res.status(500).json({
            success:false,
            message:error.message
        })
    }